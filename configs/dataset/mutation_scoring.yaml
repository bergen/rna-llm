_name_: mutation_scoring
dataset_name: mutation_scoring
csv_path:
upsample: True
max_length: 1024
d_output: 2 
batch_size: 8  # per GPU
batch_size_eval: ${.batch_size}
num_workers: 4  # For preprocessing only
add_eos: False
__l_max: ${.max_length}
shuffle: true  # set this as default!
